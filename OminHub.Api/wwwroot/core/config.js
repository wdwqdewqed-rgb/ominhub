const API_BASE = "http://localhost:8080";
const URL_BASE = "http://localhost/OminHub/OminHub.Api/wwwroot";
const BASE_PATH = (window.OMINHUB_BASE_PATH || ".").replace(/\/+$/, "");

export const Config = {
    //const
    API_BASE,
    BASE_PATH,
    URL_BASE
}